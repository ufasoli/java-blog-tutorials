### Asciidoc - Asciidoctor maven hello world

An introduction tutorial to Asciidoc and Asciidoctor using maven and the asciidoctor plugin

To build just run 

```shell
mvn clean compile 
```

and look at the generated HTML doc files under `/target/generated-docs`